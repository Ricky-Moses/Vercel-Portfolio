import Title from '../../Components/Title'
const ContactTitle = () => {
    return (
        <>
            <Title
                bgTitle='CONTACT'
                headTitle='GET IN TOUCH'
                bgFontSize='clamp(4em, 10vw, 10em)'
                headFontSize='clamp(1.5em, 5vw, 5em)'
                height='clamp(5rem, 12vw, 15rem)'
            />
        </>
    )
}

export default ContactTitle